package com.rieno.gadgetsandgizmos.lib.scratch;

// Scratch surfaces are deliberately host-only: the library never exposes a menu, keybind, or player toggle.
public enum ScratchSurfaceAccess {
    HOST_ONLY
}
